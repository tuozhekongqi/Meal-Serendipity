# AGENTS.md

本文件适用于整个仓库。所有长期协作者和自动化代理在修改项目前都必须遵守这些规则。

## 文档优先级与项目状态

- 用户当前任务中的明确要求优先于本文件；本文件优先于临时实现习惯。
- `docs/product-brief.md` 是产品目标来源，`docs/design-system.md` 是视觉与交互来源，`docs/implementation-plan.md` 是阶段边界来源。
- `docs/current-behavior-checklist.md` 记录当前可运行基线；它描述“现在是什么”，不代表所有现状都应长期保留。
- `docs/data-source-contract.md` 定义实时数据与灵感数据的边界；涉及 provider、位置、价格、距离或 ETA 的实现必须符合该契约。
- GitHub Pages API 当前报告由 `.github/workflows/pages.yml` 从 `main` 构建并部署 `dist/`。2026-08-17 的手动 Actions 部署和无缓存 HTTP 核验确认线上内容符合 `dist/` 边界。根目录 `index.html` 仍是开发源入口和 branch deployment 回滚来源；每次部署都必须复核线上 HTML 和资源路径，不能只依据 Actions 成功下结论，也不能把尚未接入的实时 Provider 或遥测能力写成已经上线的能力。

## 项目定位与核心目标

- Meal-Serendipity 是面向“今天吃什么”决策疲劳的外卖推荐助手。
- 核心目标是让用户在约 30 秒内得到一个符合预算、口味、距离、配送时间和当前状态的结果，并理解“为什么推荐”。
- 首屏应帮助用户做出一个决定，而不是展示更多平级选择；替代方案只用于解决明确取舍。
- 饮食避讳与过敏原是不可放宽的硬约束，任何候选池回退、随机路径或数据降级都不得绕过。
- 多人用餐必须保留逐人偏好和结果归属，不得悄悄合并为模糊的平均偏好。
- 推荐理由必须来自真实过滤、评分和数据字段，不得制造虚假的精确度。

## 实时模式与灵感模式

- `live` 模式只能展示合法数据源实际提供且仍在有效期内的商家、可售状态、价格、费用、距离和 ETA。
- `inspiration` 模式可以使用仓库内静态菜品数据，但必须清楚标注为“菜品灵感”；不得伪造商家、库存、距离、ETA、实时价格或可下单状态。
- 实时数据不可用、超时、过期或用户拒绝定位时，应明确说明原因并降级到灵感模式，不能无提示地混用两种数据。
- 第三方私密凭证不得进入浏览器包、仓库或 GitHub Pages。真实 provider 必须通过受控服务端或不含密钥的公开接口接入。
- 精确位置仅用于用户主动发起的当前请求；不得写入 `localStorage`、分析事件或普通日志。长期保存只能使用用户主动选择的粗粒度区域。

## 视觉与体验原则

- 已确认的目标方向是现代、简洁、舒服的“极简智能型”；具体 token、排版、组件和状态以 `docs/design-system.md` 为准。
- 传统古风海报式视觉只属于当前行为基线，不是未来设计约束。不得继续扩展宣纸、卷轴、印章、描金等装饰语言。
- 移动端优先，从 320px 宽度开始设计；桌面端增强信息组织和留白，但不能成为唯一正确体验。
- 关键操作应有清晰层级、足够对比度和至少 44×44px 的触控区域。
- 表单必须有可理解的标签、错误、帮助、加载、空状态和恢复路径；不能只依赖颜色表达状态。
- 动画只用于表达状态变化，必须支持 `prefers-reduced-motion`，且不得阻碍操作。
- 可访问性以 WCAG 2.2 AA 为基线，覆盖键盘、焦点、语义、对话框、动态播报、200% 缩放和对比度。

## 架构与代码边界

- 推荐逻辑必须与 UI 解耦。领域模型、候选过滤、评分、解释、provider、存储和 DOM 渲染应保持清晰边界。
- 核心推荐函数应尽量是可重复、可测试的纯函数；不得直接依赖 DOM、`window`、`localStorage`、网络跳转或系统时间。
- 随机逻辑应允许注入随机源；时间相关逻辑应允许注入当前时间，便于确定性测试。
- 数据结构和权重变更必须说明字段含义、迁移方式和对结果的影响，并补充相应测试。
- 根目录 `index.html` 是构建输入和 branch deployment 回滚入口；当前 GitHub Pages 生产内容已经核验为 Actions 生成的 `dist/` artifact，后续实际状态仍以线上资源核验为准。`一餐之缘_分享版/index.html` 是独立历史副本，除非任务明确要求，不得手工双向同步。
- 在对应阶段获批前，不得提前引入框架、账号体系、遥测、服务端依赖或私密第三方凭证。

## 修改流程

### 修改前

1. 阅读 `README.md`、本文件及与任务相关的 `docs/` 文档。
2. 检查工作树、当前分支、部署来源和已有改动，避免覆盖他人工作。
3. 写出简短计划，列明问题、范围、非目标、风险、验证方法和回滚点。
4. 修改页面流程、数据、推荐逻辑、存储键或部署前，先用 `docs/current-behavior-checklist.md` 固定现状。
5. 若规则冲突，先在治理文档中明确“当前实现”与“目标状态”，再修改代码；不要靠猜测解决。

### 修改中

- 严格遵守当前阶段边界，保持变更小而聚焦，不顺手重写无关代码。
- UI 文案、数据、推荐逻辑、provider、持久化和部署变更应尽量分开，便于审查和回滚。
- 不得提交密钥、个人数据、构建产物、临时文件、浏览器截图缓存或编辑器缓存。
- 不得把尚未接入的实时数据能力描述成已完成，也不得用静态数据冒充线上商家数据。

### 修改后

- 运行与变更相称的自动化检查和真实浏览器检查；不能仅凭代码阅读宣称完成。
- 每次至少运行 `git diff --check`，检查 diff 范围，并确认没有计划外文件。
- 推荐逻辑变更必须覆盖硬过滤、空候选、多人偏好、预算/距离/ETA 边界、TTL、历史去重和随机约束。
- UI 变更必须检查 320px、390px、768px、1440px，覆盖横向溢出、键盘、焦点、触控目标、文本缩放和减少动效。
- 部署变更必须验证 `/Meal-Serendipity/` 子路径、静态资源、404、刷新和线上回滚。
- 交付说明必须列出修改文件、实际运行的命令、结果、未覆盖风险和回滚基线。

## Git 与发布

- 不要直接修改或提交到 `main` 分支。
- 每个阶段从最新 `main` 创建短期分支，默认使用 `codex/` 前缀；通过 Pull Request 审查后再合并。
- 不要强推共享分支、改写他人历史或在未获授权时合并 Pull Request。
- 每阶段保持独立提交和可识别回滚点；提交信息应描述意图，Pull Request 应包含验证证据和风险说明。
- 当前 GitHub Pages Source 为 `GitHub Actions`；`.github/workflows/pages.yml` 只在检查和构建成功后发布 `dist/`。若需要回滚到 branch deployment，必须单独批准并验证 `main` 根入口、项目子路径、静态资源和 404。

## 已解决的治理冲突

- “保留古风视觉”已由确认后的极简智能型方向取代；古风只作为当前基线被记录。
- “永久零后端”已调整为“当前零后端、未来 provider 边界清晰”；未获得合法服务和单独批准前仍只能运行灵感模式。
- “直接打开即可使用”仍是当前基线能力，但未来构建方式以分阶段实施计划和 GitHub Pages 验收为准。
