# AGENTS.md

本文件适用于整个仓库。所有长期协作者和自动化代理在修改项目前都应遵守以下规则。

## 项目定位

- “一餐之缘 / Kismet Kitchen”是一个面向外卖选择困难场景的中文餐食推荐工具。
- 当前产品是零依赖、零后端、可离线打开的单文件 Web 应用；根目录 `index.html` 是当前主入口。
- 产品以轻量的中式仪式感降低选择压力，同时尊重每位用餐者的口味、预算、场景、天气和饮食避讳。
- 浏览器本地数据应保持私密；除非有明确方案和审批，不得引入遥测、账号体系或服务端依赖。

## 外卖推荐核心目标

- 首要目标是快速给出“可以直接下单”的结果，而不是让用户面对更多选择。
- 推荐必须优先满足饮食避讳和过敏原等硬约束，再考虑个人口味、主食类型、预算、天气、场景、新鲜度和大众/小众偏好。
- 多人用餐应保留逐人偏好，并清楚说明菜品对应的食客；不应把所有人的选择粗暴合并为单一平均偏好。
- “首选 / 稳妥 / 尝鲜”和“摇签”可以有不同体验，但不得绕过同一套安全过滤规则。
- 推荐理由应可理解、与实际评分或过滤依据一致，避免制造虚假的精确度。

## 视觉与交互原则

- 视觉应现代、简洁、舒服：保留克制的宣纸、朱砂、墨色、黛青和描金语汇，避免堆叠装饰、过度动画和信息噪声。
- 移动端优先，从 320px 宽度开始设计；桌面端用于增强留白和布局，不应成为唯一正确体验。
- 关键操作应有清晰层级、足够对比度和至少约 44px 的触控区域；表单、反馈、错误和空状态必须清楚。
- 动画用于表达状态变化和仪式感，必须避免阻碍操作，并考虑 `prefers-reduced-motion`。
- 不应为“重做页面”而重做页面。任何视觉调整都要说明用户问题、影响范围和验收标准。

## 架构与代码边界

- 推荐逻辑必须与 UI 解耦。新增或重构时，将菜品数据、过滤规则、评分/抽签逻辑、状态持久化和 DOM 渲染视为独立边界。
- 核心推荐函数应尽量成为可重复、可测试的纯函数；DOM、`window`、`localStorage` 和平台跳转不得渗入评分与过滤规则。
- 菜品数据应保持结构一致，并明确字段含义；调整数据或权重时必须说明对推荐结果的影响。
- 饮食避讳属于硬过滤。任何降级或候选池回退都不得悄悄恢复已被避讳的菜品。
- 根目录 `index.html` 与 `一餐之缘_分享版/index.html` 是两个独立副本。修改前必须确认哪一个是权威版本，未经明确计划不要双向手工同步。
- 在批准迁移之前，保持零依赖、无构建步骤和直接打开即可使用的能力；不要为了技术偏好引入框架。

## 修改流程

### 修改前

1. 阅读 `README.md`、本文件、相关 `docs/` 文档和将要修改的源码。
2. 检查工作树、当前分支和现有改动，避免覆盖他人工作。
3. 写出简短计划，包含问题、范围、非目标、风险、验证方法和回滚方式。
4. 对推荐规则、数据结构、存储键或页面流程的修改，先记录当前行为与关键样例。

### 修改中

- 保持变更小而聚焦，不顺手重写无关业务代码或页面。
- UI 文案、数据、推荐逻辑和持久化变更应尽量拆分，便于审查和回滚。
- 不得提交密钥、个人数据、构建产物、临时文件或编辑器缓存。

### 修改后

- 必须实际运行项目并检查浏览器控制台无新增错误。
- 至少检查 320px、常见手机宽度和桌面宽度；覆盖键盘操作、触控目标、文本溢出及动效降级。
- 手动走通：快速随缘、完整表单、单人/多人、预算、天气、避讳、三案切换、摇签、重选、刷新恢复和下单跳转。
- 推荐逻辑变更必须增加或运行确定性测试，覆盖硬过滤、候选池为空、多人偏好、预算边界、历史去重及随机抽签约束。
- 当前仓库没有 `package.json`、自动化测试或构建命令。建立自动化之前，可用本地静态服务器运行，例如 `python -m http.server 8000`，并进行上述浏览器检查。
- 提交前检查 diff，确认只包含计划内文件；在交付说明中列出运行过的命令、检查结果和未覆盖风险。

## Git 协作

- 不要直接修改或提交到 `main` 分支。
- 每项工作从最新 `main` 创建短期分支，默认使用 `codex/` 前缀；通过 Pull Request 审查后再合并。
- 不要强推共享分支，不要改写他人的提交历史，不要在未获授权时合并 Pull Request。
- 提交信息应描述意图，Pull Request 应包含变更摘要、验证证据、界面截图（如适用）和风险说明。
